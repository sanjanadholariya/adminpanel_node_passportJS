<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= single.title %> | Blog</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-light text-dark">

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
    <div class="container">
      <a class="navbar-brand fw-bold" href="/website">My Blog</a>
      <a href="/website" class="btn btn-outline-light">← Back to Blogs</a>
    </div>
  </nav>

  <div class="container my-5">

    <!-- Blog Card -->
    <div class="row justify-content-center">
      <div class="col-lg-8 col-md-10">
        <div class="card shadow-sm border-0">
          <!-- Blog Image -->
          <img src="/uploads/<%= single.image %>" class="card-img-top img-fluid rounded-top" alt="Blog Image">

          <div class="card-body">
            <!-- Blog Meta -->
            <p class="text-muted mb-2">
              <strong>Posted on:</strong> <%= new Date(single.date).toLocaleDateString('en-GB', { 
        weekday: 'long', 
        day: '2-digit', 
        month: 'short', 
        year: 'numeric' 
      }) %>
            </p>

            <!-- Title -->
            <h2 class="card-title fw-bold text-dark mb-3">
              <%= single.title %>
            </h2>

            <!-- Category -->
            <span class="badge bg-primary mb-3">
              <%= single.category %>
            </span>

            <!-- Description -->
            <p class="card-text text-secondary" style="text-align: justify;">
              <%= single.description %>
            </p>

          </div>
        </div>
      </div>
    </div>

  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
